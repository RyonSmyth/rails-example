<h1 class="header">Listing products</h1>
<h2> </h2>
<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Description</th>
      <th>Image url</th>
      <th>Color</th>
      <th>Price</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @products.each do |product| %>
      <tr id="product-design">
        <td class="product-spacing" id="product-content"><%= product.name %></td>
        <td class="product-spacing" id="product-content"><%= product.description %></td>
        <td id="product-content"><img src="<%= product.image_url %>" id="product-image"></td>
        <td class="product-spacing" id="product-content"><%= product.color %></td>
        <td class="product-spacing" id="product-content"><%= product.price %></td>
        <td class="product-spacing" id="product-features"><%= link_to 'Show', product %></td>
        <td class="product-spacing" id="product-features"><%= link_to 'Edit', edit_product_path(product) %></td>
        <td class="product-spacing" id="product-features"><%= link_to 'Destroy', product, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<p class="new-product"><%= link_to 'New Product', new_product_path %></p>
